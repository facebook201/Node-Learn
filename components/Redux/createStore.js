
import $$observable from './utils/symbol-observable';





/**
 * 创建 store
 * @param reducer 纯函数 返回下个 state 状态树
 * 
 * @return 返回 redux store
 **/
function createStore(reducer, preloadedState, enhancer) {
    
}